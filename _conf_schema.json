{
  "hermes_command": {
    "description": "Hermes CLI 命令路径",
    "type": "string",
    "default": "hermes",
    "hint": "例如: hermes 或 /usr/local/bin/hermes"
  },
  "hermes_workdir": {
    "description": "Hermes 工作目录",
    "type": "string",
    "default": "",
    "hint": "留空则使用 Hermes 默认工作目录。例如: D:/AI/workspace"
  },
  "hermes_model": {
    "description": "创建新会话时使用的默认模型",
    "type": "string",
    "default": "",
    "hint": "留空则使用 Hermes 默认模型。例如: deepseek/deepseek-v4-flash"
  },
  "max_timeout": {
    "description": "Hermes 命令最大超时时间（秒）",
    "type": "int",
    "default": 120
  },
  "quick_prefix": {
    "description": "快捷发送前缀",
    "type": "string",
    "default": ">",
    "hint": "前缀后需加空格，如「> 消息内容」发送到当前会话"
  },
  "output_mode": {
    "description": "输出模式：simple（只返回最终回复） / verbose（包含中间思考）",
    "type": "string",
    "default": "simple",
    "options": ["simple", "verbose"]
  },
  "auto_create_session": {
    "description": "收到快捷前缀消息时，如果没有当前会话则自动创建新会话",
    "type": "bool",
    "default": true
  },
  "default_system_prompt": {
    "description": "创建新会话时的默认系统提示词（覆盖 Hermes 内置 persona）",
    "type": "string",
    "default": "",
    "hint": "留空则使用 Hermes 默认 persona"
  },
  "require_approval": {
    "description": "审批模式。all=每次都需要审批, smart=智能审批(低风险自动放行), off=不审批",
    "type": "string",
    "default": "smart",
    "options": ["all", "smart", "off"],
    "hint": "smart 模式：读取/搜索等低风险操作自动放行，删除/覆盖等高风险操作需要确认"
  },
  "poke_approve": {
    "description": "戳一戳机器人自动审批全部待审批请求（仅 QQ NapCat）",
    "type": "bool",
    "default": true,
    "hint": "开启后在 QQ 戳一戳机器人自动批准所有待审批请求"
  },
  "approval_timeout": {
    "description": "审批超时时间（秒）",
    "type": "int",
    "default": 60,
    "hint": "超过此时间未审批则自动拒绝"
  },
  "auto_report": {
    "description": "任务完成后自动汇报简短摘要",
    "type": "bool",
    "default": true,
    "hint": "Hermes 执行完后自动总结做了什么，而不是只返回原始回复"
  },
  "auto_report_max_length": {
    "description": "汇报摘要最大长度（字符数）",
    "type": "int",
    "default": 500,
    "hint": "自动汇报时截断过长回复"
  },
  "hermes_approval_mode": {
    "description": "Hermes CLI 审批模式。yolo=跳过所有危险命令确认，normal=默认（需确认），plan=规划模式（不执行）",
    "type": "string",
    "default": "normal",
    "options": ["normal", "yolo"],
    "hint": "yolo 模式会自动批准 Hermes 侧的敏感操作（文件写入、命令执行等），请谨慎使用"
  }
}
